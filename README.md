# Hướng dẫn dự án

## Cách chạy Hugo
Để chạy server phát triển (development server) trên máy cục bộ, hãy mở terminal và chạy lệnh sau:

```bash
hugo server
```

## Hướng dẫn chỉnh sửa
**Quan trọng:** Để chỉnh sửa nội dung và giao diện, bạn chỉ cần mở và sửa trực tiếp file **`public/index.html`**.
